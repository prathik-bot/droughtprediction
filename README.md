# drought prediction
droughtprediction backend
Install Steps
/usr/local/bin/python3.10 -m venv drought

source drought/bin/activate
pip install -r requirements.txt

#New machine config

git remote set-url origin git@github.com-prathik:prathik-bot/droughtprediction.git

ssh -T github.com-prathik

#setup ssh config as above

